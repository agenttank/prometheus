# my prometheus stack for portainer

- needs some yml files with secrets - see volumes of each service

./alertmanager/config.yml
./pve-exporter/pve.yml
./idrac_exporter/idrac.yml
./blackbox_exporter/blackbox.yml
